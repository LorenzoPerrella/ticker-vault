# Copia in `.env.secret` (gitignored, in questa stessa cartella) con
# credenziali di produzione — distinte da quelle di dev, mai le stesse.
POSTGRES_USER=price_service
POSTGRES_PASSWORD=change-me-in-a-real-secret-manager
POSTGRES_DB=price_service
